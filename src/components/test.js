import React, { Component } from 'react';
import { View, Text } from 'react-native';

const Test = (props) => {
    return (
        <Text>Hello! {props.something}</Text>
    );
}

export default Test;


